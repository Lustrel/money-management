{% extends "base.html.twig" %}

{% block title %}
    {{ parent() ~ 'Editar cliente' }}
{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Editar cliente</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}